# Backend

Backend API server for the Puller project.

## Directory Structure

- `src/` - Source code
  - `controllers/` - Request handlers and business logic
  - `models/` - Data models and schemas
  - `routes/` - API route definitions
  - `middleware/` - Custom middleware functions
  - `utils/` - Helper functions and utilities
- `config/` - Configuration files
- `tests/` - Backend tests

## Getting Started

### Installation

```bash
npm install
```

### Development

```bash
npm run dev
```

### Testing

```bash
npm test
```

## Technology Stack

TBD - Configure your preferred backend framework (Express, Fastify, NestJS, etc.)

## API Documentation

API documentation will be available at `/api/docs` when the server is running (once configured).
